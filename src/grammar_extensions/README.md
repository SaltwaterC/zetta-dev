# Additional syntax grammars

This directory contains Zetta-owned language configuration and Tree-sitter
highlight queries for grammars that are not included in Zed's upstream grammar
bundle. The parser implementations and source queries are supplied or adapted
from the MIT-licensed `tree-sitter-make` and `tree-sitter-toml-ng` projects.
